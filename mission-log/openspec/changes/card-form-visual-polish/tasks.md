## 1. Color overlay

- [x] 1.1 Add `placement` to `PopoverBase` and `variant="button"` on `ColorPicker` (swatches + hex in one float); verify the project form still uses the panel
- [x] 1.2 Add `cardSurfaceStyle` and use it on the form card, save button (live), and public card

## 2. Form chrome

- [x] 2.1 Make `CardFormView` one column with the color button overlaid on the right; chips stay `w-auto`
- [x] 2.2 Fix `CardProjectSelect` dark-mode text; rectangular add-section on the right; quieter delete

## 3. Public theme

- [x] 3.1 Add a single-button theme toggle and place it top-right on `CardPublicView` with the same color tint
